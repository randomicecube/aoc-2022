#include <algorithm>
#include <fstream>
#include <iostream>
#include <string>
#include <vector>

int main() {
  std::ifstream input("resources/aoc-input.txt");
  std::string line;
  while (std::getline(input, line)) {
    // do stuff
  }
  return 0;
}